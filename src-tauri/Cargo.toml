[package]
name = "photonflow"
version = "0.1.0"
description = "PhotonFlow Desktop App"
authors = ["you"]
edition = "2021"

[package.metadata.tauri]
product_name = "PhotonFlow"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
